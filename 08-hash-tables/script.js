'Use strict';
// NOTE Core concepts
/*
1. HASH TABLE: a list of KEY-VALUE pairs.

2. HASHING: the PROCESS of taking CHARACTERS and coverting them to NUMBERS.

3. HASH FUNCTION: the CODE that is used to convert those letters into particular numbers.

4. ONE criterion required for a VALID hash function:
A hash function MUST convert the SAME string to the SAME number every single time it's applied.



*/
/////////////////////////////////////////////////
